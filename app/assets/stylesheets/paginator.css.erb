/*csslint box-model:false*/
/*TODO: Remove this lint rule override after a refactor of this code.*/


.simple-paginator {

    /* `simple-g` Grid styles */
    letter-spacing: -0.31em; /* Webkit: collapse white-space between units */
    *letter-spacing: normal; /* reset IE < 8 */
    *word-spacing: -0.43em; /* IE < 8: collapse white-space between units */
    text-rendering: optimizespeed; /* Webkit: fixes text-rendering: optimizeLegibility */

    /* `simple-paginator` Specific styles */
    list-style: none;
    margin: 0;
    padding: 0;
}
.opera-only :-o-prefocus,
.simple-paginator {
    word-spacing: -0.43em;
}

/* `simple-u` Grid styles */
.simple-paginator li {
    display: inline-block;
    *display: inline; /* IE < 8: fake inline-block */
    zoom: 1;
    letter-spacing: normal;
    word-spacing: normal;
    vertical-align: top;
    text-rendering: auto;
}


.simple-paginator .simple-button {
    border-radius: 0;
    padding: 0.8em 1.4em;
    vertical-align: top;
    height: 1.1em;
}
.simple-paginator .simple-button:focus,
.simple-paginator .simple-button:active {
    outline-style: none;
}
.simple-paginator .prev,
.simple-paginator .next {
    color: #C0C1C3;
    text-shadow: 0 -1px 0 rgba(0,0,0, 0.45);
}
.simple-paginator .prev {
    border-radius: 2px 0 0 2px;
}
.simple-paginator .next {
    border-radius: 0 2px 2px 0;
}
